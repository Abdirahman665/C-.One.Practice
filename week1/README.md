# Discouse chapter 1
# week1 - C# Home work Assignment2

## 1 is has variables

string name = textname.Text;
int id = int.Parse(textId.Text);
string department = textPartment.Text;
int semester = int.Parse(textSemester.Text);
string output;

## 2 is has contination

output = name + ", " + id + ", " + department + ", " + semester;

lbioutput.Text = output;

## 3 is has Display output

lbioutput.Text = output;


## 4 is has clear

lbioutput.Text = "";